/**
 * RunDetailsPage — thin wrapper that renders the RunDetails component.
 * The actual logic lives in components/eval/RunDetails.jsx.
 */
import RunDetails from '../components/eval/RunDetails.jsx'

export default function RunDetailsPage() {
  return <RunDetails />
}
